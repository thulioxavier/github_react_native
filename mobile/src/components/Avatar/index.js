import React from 'react';
import { Img } from './styles';

const Avatar = ({source}) => {
    return(
        <>
        <Img
            source={source}
        />
        </>
    );
}

export default Avatar;